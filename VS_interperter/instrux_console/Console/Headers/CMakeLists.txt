# Add header files
set(HEADER_FILES
    "console.h"
)

# Optionally, you can create a header-only library
# add_library(console_headers INTERFACE)
# target_include_directories(console_headers INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}")
